name = "front-porch-economics-api"
main = "src/worker.js"
compatibility_date = "2024-01-01"

[[d1_databases]]
binding = "DB"
database_name = "front-porch-economics"
database_id = "86bd6964-3270-4c00-9bdc-2e8299c08c52"

# Required secrets — set once via Wrangler, never commit values:
#   npx wrangler secret put JWT_SECRET          (random 32+ char string)
#   npx wrangler secret put TWILIO_ACCOUNT_SID
#   npx wrangler secret put TWILIO_AUTH_TOKEN
#   npx wrangler secret put TWILIO_FROM_NUMBER  (E.164 format, e.g. +16175551234)
